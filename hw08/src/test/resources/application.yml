spring:
  data:
    mongodb:
      host: localhost
      #port: 0
      database: test

mongock:
  enabled: false
logging:
  level:
    org.springframework.data.mongodb: DEBUG
    de.flapdoodle: DEBUG