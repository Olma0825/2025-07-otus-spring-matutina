spring:
  data:
    mongodb:
      host: localhost
      #port: 0
      database: test
de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.0.2
mongock:
  enabled: false
logging:
  level:
    org.springframework.data.mongodb: DEBUG
    de.flapdoodle: DEBUG