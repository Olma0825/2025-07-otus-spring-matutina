spring:
  datasource:
    url: jdbc:h2:mem:testdb
  sql:
    init:
      mode: never
  flyway:
    enabled: true
    locations:
    - classpath:db/migration
    - classpath:db/testdata
    baseline-on-migrate: false
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true